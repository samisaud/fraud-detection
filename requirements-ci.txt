# Lightweight deps for CI — excludes heavy libs not needed for unit tests
# (streamlit, plotly, shap, dvc, mlflow, lightgbm are excluded)
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
imbalanced-learn>=0.12
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.5
httpx>=0.27
pyyaml>=6.0
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
